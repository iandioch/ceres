Requires a file, `config.hidden.json`, with JSON in the following format:

```
{
    "consumer_key": "abc",
    "consumer_secret": "def",
    "access_token": "ghi",
    "access_token_secret": "jkl"
}
```
